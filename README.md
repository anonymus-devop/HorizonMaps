# HorizonMaps - corrected minimal project

This archive contains a minimal, corrected HorizonMaps project skeleton ready for `npm install` and `npm run build`.

- Fill VITE_MAPBOX_TOKEN as environment variable for Mapbox usage.
- This skeleton intentionally contains no assets.
